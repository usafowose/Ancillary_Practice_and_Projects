#include <stdio.h>

int main(void)
{
  printf("Meow\n");
  printf("Meow\n");
  printf("Meow\n");

  // int counter = 0;
  // counter += 2;
  // printf("Number is %d", counter);

  // LOOPS
  int counter = 3;
  while (counter > 0)
  {
    printf("meow\n");
    counter--;
  }
}
